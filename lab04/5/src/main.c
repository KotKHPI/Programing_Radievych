#define X -0.2 // записуємо значення х
int main() {
	float y;

	if (X <= -1) {
	y = -X -1; //значення функції на проміжку х <= -1
}
	if (X > -1 && X <= 0) {
	y = X + 1; //значення функції на проміжку від х > -1 до x <= 0
}
	if (X > 0 && X < 1) {
	y = -X + 1; //значення функції на проміжку від х > 0 до x < 1
}
	if (X >= 1) {
	y = X - 1; //значення функції на проміжку х >= 1
}

	return 0;
}
