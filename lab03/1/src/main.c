#include "lib.h"
int main()
{
	int r1, r2, r3, R; // усі резистори
	r1 = 4; // опір першого резистора
	r2 = 1; // опір другого резистора
	r3 = 3; // опрі третьго резистора
	R = r1 + r2 + r3; // опір трьох резисторів при паралельному з'єднанні
	return 0;
}
