#include "lib.h"
int main()
{
	int a = 1; // это а1
	int b = 6; // это а2
	int d = 1; // это разность ар пр
	int c; // коеф разности
	int q; // число, которое получится при сумме
	c = b - a;
	q = ((a + b)/2 * c) + b; // сумма ар пр
	
	return 0;
}
